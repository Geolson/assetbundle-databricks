resources:
  pipelines:
    pipeline_demo_pipeline:
      name: demo_pipeline
      libraries:
        - glob:
            include: ../../src/pipelines/demo_pipeline/transformations/**
      schema: information_schema
      continuous: false
      development: false
      photon: true
      channel: CURRENT
      catalog: ${var.calalog_name}
      serverless: true
      root_path: ../../src/pipelines/demo_pipeline
